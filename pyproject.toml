[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cast-ollama"
version = "0.1.0"
description = "cAST Efficient Ollama Demo"
authors = [
    {name = "User", email = "user@example.com"},
]
dependencies = [
    "oracledb>=2.0.0",
    "sentence-transformers>=2.2.0",
    "torch>=2.0.0",
    "tree-sitter>=0.20.0",
    "tree-sitter-python>=0.21.0",
    "FlagEmbedding>=1.5.0",
    "ollama>=0.1.0",
    "pydantic>=2.0.0",
    "click>=8.0.0",
    "tabulate>=0.9.0",
    "matplotlib>=3.5.0",
    "pandas>=1.5.0",
    "numpy>=1.24.0",
    "requests>=2.28.0",
    "python-dotenv>=1.0.0",
    "PyYAML>=6.0",
    "tqdm>=4.60.0",
    "chromadb>=0.5.0",
]
requires-python = ">=3.10"

[project.scripts]
cast-ollama = "cast_ollama.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
